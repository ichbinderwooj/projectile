#include <cstdio>

int main()
{
    float mass, drag_coefficient, air_density, area;

    printf("Mass: ");
    scanf_s("%f", &mass);
    printf("Drag Coefficient: ");
    scanf_s("%f", &drag_coefficient);
    printf("Air density: ");
    scanf_s("%f", &air_density);
    printf("Cross-sectional Area: ");
    scanf_s("%f", &area);

    return 0;
}